import ContainerStyled from "../shared/ContainerStyled";

const App = (): React.ReactElement => {
  return (
    <ContainerStyled>
      <p>Contact-in app</p>
    </ContainerStyled>
  );
};

export default App;
